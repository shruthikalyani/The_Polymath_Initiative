# Install these into a CUDA-enabled Google Colab runtime. Colab provides the
# CUDA-compatible PyTorch build, so torch is intentionally not pinned here.
accelerate>=1.0
bitsandbytes>=0.43
transformers>=4.45
sentencepiece>=0.2
safetensors>=0.4
tiktoken>=0.8
numpy>=1.26
tqdm>=4.66
regex>=2024.9.11
nltk>=3.9
bert-score>=0.3.13
requests>=2.32
python-dotenv>=1.0
# Imported by the existing LoCoMo utilities, even though this local runner
# never makes API calls.
groq>=1.0
openai>=1.0
google-generativeai>=0.8
anthropic>=0.40
